{
  "name": "url-shortener",
  "version": "1.0.0",
  "description": "Encurtador de URLs com React e Go",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && go run main.go",
    "frontend": "cd frontend && npm run dev",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}